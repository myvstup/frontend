div(layout='column')
  md-button.md-accent( flex='25', flex-offset='75',flex-offset-xs='50', ui-sref='home') Почати з початку
md-divider

h2 Field choice
md-content.md-padding(layout='column')
  form()
    md-autocomplete(
      md-selected-item='vm.choice.city',
      md-items='city in vm.filter(vm.specData, searchCity)',
      md-item-text='city.name',
      md-search-text='searchCity',
      md-search-text-change='vm.convert(city)',
      md-selected-item-change='vm.convert(city)',
      required,
      md-min-length='0',
      placeholder='Введ1ть м1сто')
      md-item-template
        span(md-highlight-text='searchText') {{city.name}}

    md-divider.md-padding

    
    
    md-autocomplete(
      ng-disabled="!vm.choice.city"
      md-selected-item='vm.choice.field',
      md-items='specialization in vm.filter(vm.choice.city.specializationList, searchUniver)',
      md-item-text='specialization',
      md-search-text='searchUniver',
      required,
      md-min-length='0',
      md-search-text-change='vm.convert(specialization)',
      md-selected-item-change='vm.convert(specialization)',
      placeholder='Введ1ть напрям')
      md-item-template
        span(md-highlight-text='searchUniver') {{specialization}}

    //- input(type='text', step='any', name='rate', ng-model='vm.userData.uniName')
    //- input(type='number', step='any', name='rate', ng-model='vm.userData.facultatyName')
    //- input(type='number', step='any', name='rate', ng-model='vm.userData.specialityName')
    //- input(type='number', step='any', name='rate', ng-model='vm.userData.fieldName')

md-button( flex='45', flex-xs='100', ng-click='vm.postChoice(vm.userData)') 
  | Get answers

md-button.md-primary( flex='45', flex-xs='100', ng-click='vm.getSpecList()') 
  | Get answers
  
div
  h2 ANSWER
  .md-headline(ng-if="!vm.specializations") no data!!!
  div(ng-repeat="spec in vm.specializations")
    p М1сто: {{spec.cityName}}
    p Ун1верситет: {{spec.universityName}}
    p Рейтинг ун1верситету: {{spec.universityRank}}
    p Факультет: {{spec.facultatyName}}
    p Спец1альн1сть: {{spec.specialityName}}
    p Ймов1рн1сть вступу: {{spec.specProbability}}
    md-divider.md-padding
